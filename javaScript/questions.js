// Array of questions
let questions = [
    {question:'Inside which HTML element do we put the JavaScript?', 
    answer:'script', 
    options:['JS', 'JavaScript', 'script', 'scripting']},
    {question:'Which operator returns true if the two compared values are not equal?', 
    answer:'!==', 
    options:['<>', '==!', '~', '!==']},
    {question:'How is a forEach statement different from a for statement?', 
    answer:'A for statement is generic, but a forEach statement can be used only with an array.', 
    options:['Only a for statement uses a callback function.', 'A for statement is generic, but a forEach statement can be used only with an array.', 'Only a forEach statement lets you specify your own iterator.', 'A forEach statement is generic, but a for statement can be used only with an array.']},
    {question:'Which statement is the correct way to create a variable called rate and assign it the value 100?', 
    answer:'let rate = 100;', options:['let rate = 100;', 'let 100 = rate;', '100 = let rate;', 'rate = 100;']},
    {question:'Which property references the DOM object that dispatched an event?', answer:'target', 
    options:['self', 'object', 'target', 'source']}
];